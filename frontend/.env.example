VITE_API_BASE_URL=http://localhost:7869/api/v1
# local: 仅本地语录
# api: 仅走后端 /api/v1/quote/random（失败回退本地）
# hitokoto: 仅走 https://v1.hitokoto.cn/（失败回退本地）
# auto: 优先 hitokoto -> 后端 -> 本地（推荐）
VITE_QUOTE_SOURCE=auto
